#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
    printf("Process ID: %d\n", getpid());
    printf("Parent Process ID: %d\n", getppid());

    char *args[] = {"/bin/ls", NULL};
    execv(args[0], args);

    return 0;
}
